# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET CI/CD

on:
  push:
  pull_request:

jobs:
  build-and-test:
    name: Build and test
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Setup dotnet
        id: setup-dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.200'
      - name: Set dotnet version
        run: dotnet new globaljson && echo '${{ steps.setup-dotnet.outputs.dotnet-version }}'
      - name: Install dependencies
        working-directory: DotnetMultiverse
        run: dotnet workload install wasm-tools && dotnet restore
      - name: Build
        working-directory: DotnetMultiverse
        run: dotnet build -c Release --no-restore    
      - name: Ensure browsers are installed
        working-directory: DotnetMultiverse/MJ.PWA.AudioConverter.Test
        run: pwsh bin/Release/net9.0/playwright.ps1 install --with-deps
      - name: Publish PWA
        working-directory: DotnetMultiverse/MJ.PWA.AudioConverter
        run: dotnet publish -c Release --no-restore
      - name: Test
        working-directory: DotnetMultiverse
        run: dotnet test --no-restore